# DATABASE
DATABASE_URL="postgresql://postgres:postgres_password@localhost:5432/don_ne_db?schema=public"

# AUTH & SECURITY
JWT_SECRET="CHANGE_ME_JWT_SECRET_32_BYTES_MINIMUM"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# The first SUPER_ADMIN is provisioned once with temporary process-only
# ADMIN_INITIAL_EMAIL, ADMIN_INITIAL_PASSWORD, and ADMIN_INITIAL_NAME values.
# Do not store those values in this file or a normal runtime .env file.

# NEXT_PUBLIC BRAND DEFAULTS (CAN BE OVERRIDDEN IN GLOBAL SETTINGS DB)
NEXT_PUBLIC_DEFAULT_HOTLINE="0964.182.330"
NEXT_PUBLIC_DEFAULT_HOTLINE_2="0973.62.62.46"
