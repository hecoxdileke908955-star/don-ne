const { PrismaClient } = require('@prisma/client');
const base = process.env.E2E_BASE_URL, password = process.env.ADMIN_BOOTSTRAP_PASSWORD;
async function main() {
 const login=await fetch(`${base}/api/admin/auth`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({password})}); const cookie=login.headers.get('set-cookie')?.split(';')[0]; if(!login.ok||!cookie)throw Error('login'); const h={cookie,'content-type':'application/json'};
 const unauth=await fetch(`${base}/api/admin/pricing`); const read=await fetch(`${base}/api/admin/pricing`,{headers:h}); const item=(await read.json()).items[0]; const original=Number(item.minPrice), changed=original+123;
 const payload={itemName:item.itemName,unit:item.unit,minPrice:changed,maxPrice:item.maxPrice===null?null:Number(item.maxPrice),conditionText:item.conditionText,note:item.note,sortOrder:item.sortOrder,status:item.status}; const noAuth=await fetch(`${base}/api/admin/pricing/${item.id}`,{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify(payload)}); const patch=await fetch(`${base}/api/admin/pricing/${item.id}`,{method:'PATCH',headers:h,body:JSON.stringify(payload)}); const again=await fetch(`${base}/api/admin/pricing`,{headers:h}); const after=(await again.json()).items.find(x=>x.id===item.id); const invalid=await fetch(`${base}/api/admin/pricing/${item.id}`,{method:'PATCH',headers:h,body:JSON.stringify({...payload,minPrice:-1})}); const range=await fetch(`${base}/api/admin/pricing/${item.id}`,{method:'PATCH',headers:h,body:JSON.stringify({...payload,minPrice:2,maxPrice:1})}); const malformed=await fetch(`${base}/api/admin/pricing/${item.id}`,{method:'PATCH',headers:h,body:JSON.stringify({minPrice:'bad'})}); const missing=await fetch(`${base}/api/admin/pricing/missing-price-item`,{method:'PATCH',headers:h,body:JSON.stringify(payload)}); const p=new PrismaClient(); const db=await p.priceItem.findUnique({where:{id:item.id}});await p.$disconnect();console.log(JSON.stringify({login:login.status,unauth:unauth.status,noAuth:noAuth.status,read:read.status,id:item.id,original,changed,patch:patch.status,db:Number(db.minPrice),reload:Number(after.minPrice),invalid:invalid.status,range:range.status,malformed:malformed.status,missing:missing.status}));
} main().catch(e=>{console.error(e);process.exitCode=1});
